workflows:
    id: increments
    created_at: timestamps
    updated_at: timestamps

workflow_nodes:
    id: increments
    workflow_id: unsigned integer
    order: unsigned integer
    title: string
    created_at: timestamps
    updated_at: timestamps
